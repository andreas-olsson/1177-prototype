import { IDSIconShieldLockedPrimary } from './icon-shield-locked-primary-element';
declare global {
    interface HTMLElementTagNameMap {
        'ids-icon-shield-locked-primary': IDSIconShieldLockedPrimary;
    }
}
