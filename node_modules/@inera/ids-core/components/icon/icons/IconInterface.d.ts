import { TemplateResult } from "lit";
import IconProps from "./IconProps";
export default interface IconInterface {
    getDefaultIconProps: () => IconProps;
    render: (props: IconProps) => TemplateResult<1>;
}
