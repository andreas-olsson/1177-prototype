import { LitElement } from 'lit';
import { GroupErrorController } from '../controller/GroupErrorController';
import '../error-message/register';
export declare class IDSGroupBase extends LitElement {
    name: string;
    required: boolean;
    novalidation: boolean;
    legend: string;
    compact: boolean;
    inputs: Array<Element>;
    protected inputSelector: string;
    protected groupErrorController: GroupErrorController;
    static styles: any[];
    firstUpdated(_changedProperties: Map<string | number | symbol, unknown>): void;
    render(): import("lit-html").TemplateResult<1>;
}
