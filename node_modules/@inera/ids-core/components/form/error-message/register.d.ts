import { IDSErrorMessage } from './error-message-element';
import '../../icons/warning/register';
declare global {
    interface HTMLElementTagNameMap {
        'ids-error-message': IDSErrorMessage;
    }
}
