import { LitElement } from 'lit';
import { FocusTrapController } from '../../../controllers/FocusTrapController';
export declare class IDSPopoverContent extends LitElement {
    static styles: import("lit").CSSResult[];
    protected focusTrapController: FocusTrapController | null;
    nofocustrap: boolean;
    maxwidth: string;
    maxheight: string;
    autofocus: boolean;
    position: 'top' | 'top-left' | 'top-right' | 'left' | 'left-top' | 'left-bottom' | 'right' | 'right-top' | 'right-bottom' | 'bottom' | 'bottom-right' | 'bottom-left';
    visible: boolean;
    srclosetext: string;
    protected firstUpdated(_changedProperties: Map<string | number | symbol, unknown>): void;
    disconnectedCallback(): void;
    updated(changedProperties: Map<string, unknown>): void;
    detectEsc: (event: any) => void;
    closePopover(): void;
    setSize(): Promise<void>;
    setOffset(): void;
    render(): import("lit-html").TemplateResult<1>;
}
