import { IDSPopover } from './popover-element';
import { IDSPopoverContent } from './content/popover-content-element';
declare global {
    interface HTMLElementTagNameMap {
        'ids-popover': IDSPopover;
        'ids-popover-content': IDSPopoverContent;
    }
}
